# ========== DATABASE (H2) ==========
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# ========== JPA/HIBERNATE ==========
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# ========== EMAIL (MOCK) ==========
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# ========== JWT ==========
# Clave secreta para tests (debe ser larga para HS256/RS256 si se usa)
# En este caso usamos RSA, pero para tests podemos mockear o usar una generada
# Si el código usa claves RSA generadas al vuelo, no necesitamos configurar nada aquí
